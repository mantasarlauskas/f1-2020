import React, { memo } from 'react';
import './Menu.less';

function Menu() {
    return (
        <div className="components-Menu">
            Menu placeholder
        </div>
    );
}

export default memo(Menu);
